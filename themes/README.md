## open theme & download icarus 
```sh
cd themes && git clone https://github.com/Thunf/hexo-theme-icarus.git icarus
```

## open icarus & config for uself
```sh
cd icarus && cp _config.thunf.yml _config.yml
```